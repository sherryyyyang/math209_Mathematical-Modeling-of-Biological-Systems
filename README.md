# 209 — Quantitative Biology & Systems Modeling

This folder was restored after a move. Here’s what it contains:

- **`github-codebase/`** — Seven projects (Project 1–7). For figures that match the report PDFs, run `python scripts/extract_figures_from_pdfs.py` (requires poppler). To regenerate from code (Projects 1–5 only), run `python scripts/generate_figures.py`.
- **`showcase-website/`** — Single-page site that displays those figures. Open `index.html` or run `python3 -m http.server 8080` inside it.

**Original PDFs** (e.g. combined_hw1.pdf, hw_2.pdf, …) are not in this repo. If you have them in another location after the move, copy them back here if you want to keep them with the code.
