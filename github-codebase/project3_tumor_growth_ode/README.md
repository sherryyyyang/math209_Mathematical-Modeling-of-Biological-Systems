# Project 3 — Tumor growth ODE with drug PK

Multi-compartment tumor + drug concentration c(t). Runge–Kutta.

## Run

```bash
cd project3_tumor_growth_ode
pip install numpy scipy matplotlib
python run_tumor_ode.py
```
